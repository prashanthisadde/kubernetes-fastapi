apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.mongodb.db_host }}
  labels:
    app: products
    tier: database
spec:
  ports:
    - port: 27017
  selector:
    app: products
    tier: database
